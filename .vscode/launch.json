{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launch Tests",
      "type": "node-terminal",
      "command": "yarn test",
      "request": "launch",
      "localRoot": "${workspaceFolder}"
    }
  ]
}
