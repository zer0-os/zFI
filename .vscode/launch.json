{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launch Tests",
      "type": "node-terminal",
      "command": "yarn test",
      "request": "launch",
      "localRoot": "${workspaceFolder}"
    },
    {
      "name": "Launch Script",
      "type": "node-terminal",
      // Modify this command to execute the script you're debugging
      "command": "npx hardhat run ./scripts/registerPools.ts",
      "request": "launch",
      "localRoot": "${workspaceFolder}"
    }
  ]
}
